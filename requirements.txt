# Tested with the versions below (Python 3.9+). Newer minor versions of
# these packages should also work; torch is the only one where the exact
# build matters (CPU vs CUDA) -- install the variant that matches your
# hardware from https://pytorch.org/get-started/locally/ if the plain
# `pip install torch` below does not pick the right one.
numpy==1.23.5
pandas==2.0.3
scipy==1.10.1
scikit-learn==1.3.2
torch==2.4.1
matplotlib==3.7.5
openpyxl==3.1.5
